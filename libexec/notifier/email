#!/bin/sh
[ -e "${EMAIL_ADDRESS}" ] \
  || ( echo "EMAIL_ADDRESS is undefined" >&2 && exit 1 )

set -eu

EMAIL_SUBJECT=${EMAIL_SUBJECT:-${TASK_NAME}: state ${TASK_STATE}}

cat <<EOF | mail -s "${EMAIL_SUBJECT}" "${EMAIL_ADDRESS}"
${RESULT_OUTPUT}
---
$(timeout 0.2 cat)
EOF
